import 'package:flutter/material.dart';

class PageChartDetailedPerf extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page Chart Detailed Performance'),
      ),
      body: Center(
        child: Text('This is the PageChartDetailedPerf page.'),
      ),
    );
  }
}
